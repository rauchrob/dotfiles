[include]
path = ~/.gitconfig.local

[core]
excludesfile = ~/.gitignore_global

[push]
default = matching

[alias]
ci = commit
st = status
co = checkout

[external]
diff = meld

[credential]
helper = osxkeychain

[identity "gns-systems"]
	name = Robert Rauch (GNS Systems GmbH)
	email = robert.rauch@gns-systems.de
[identity "private"]
	name = Robert Rauch
	email = mail@robertrauch.de
